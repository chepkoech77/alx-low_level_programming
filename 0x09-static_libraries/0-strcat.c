#include "main.h"
#include <string.h>
/**
 * _strcat - returns the pointer if concatenated string
 * @dest: first string
 * @src: second string
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));

	return (0);
}
